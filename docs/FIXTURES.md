# FIXTURES.md

The single registry of test fixtures. Every fixture is registered here and referenced
from here.

~~**Do not enumerate fixture names in build plan detail or in `CLAUDE.md`.** A list
kept in two places goes silently incomplete the moment one is registered in the other,
and the copy that goes stale is the one nobody is looking at. A spent prompt may name
a fixture, because a prompt records what was asked rather than what is true.~~
[moved, N.10] What other documents may say about fixtures is stated once, in
`CLAUDE.md` §9. It was stated here as well, and the two copies diverged the moment one
of them was narrowed.

~~Nothing is registered yet. Phase 0 opens this file.~~ [opened, 0.8] Three fixtures
are registered below, all of them from phase 0's rails. Each exists to make a test
fail on purpose, because a conformance test that has never failed has not been tested.

## Registry

| Fixture | Registered | Purpose | Used by |
|---|---|---|---|
| Conflicting registry | 0.4 | Two components both claiming `run_log.Insert`, which is the case INVARIANT 10 exists to catch | `WriteOwnershipConformanceTests.TheConformanceTestFailsOnADeliberatelyConflictingRegistry` |
| Attribution split registry | 0.4 | `CandidateAllocator` inserting `attribution` and `ForwardReturnFiller` updating it, asserted NOT to be a conflict. The case a per-table rule gets wrong and a per-operation rule gets right | `WriteOwnershipConformanceTests.TwoComponentsOnTheSameTableWithDifferentOperationsIsNotAConflict` |
| Trespassing stage | 0.7 | A stage declaring `run_log` and reading `security`. Both tables exist, so without the guard it succeeds and returns rows | `StageRunnerTests.AStageThatReachesOutsideItsDeclaredSetFailsTheRunAndSaysSo` |
| Uncatalogued component | 1.11 | A registry naming `NoOpStage`, which `ARCHITECTURE.html` §3 does not have. The exact name that was registered and uncatalogued through all of phase 0, so the check is exercised against the case it was written for | `RegistryNameTests.AComponentTheCatalogueDoesNotNameIsCaught` |
| Column outside the declared set | 1.12 | A stage declaring three columns of `price_daily` and bulk-loading four. `TableWrite.Columns` was declared and asserted by nothing from phase 0 until this | `BulkUpsertTests.AStageWritingAColumnItDidNotDeclareThrowsBeforeAnythingOpens` |
| Conflict target the write does not supply | 1.12 | `ON CONFLICT` on a column the bulk load omits. Valid SQL that never fires, so every re-run inserts a duplicate while succeeding, which is D-68 silently not holding | `BulkUpsertTests.AConflictTargetOutsideTheWrittenColumnsIsRefused` |
| Paged set an exact multiple of the page size | 1.1 | 100 rows in four pages of 25, where the last full page is indistinguishable from a middle one. A loop terminating on a short page asks for one more; termination keys on `links.next` | `EodhdClientTests.PagingTerminatesOnTheAbsentNextLinkWhenTheTotalIsAnExactMultiple` |
| ~~Paged read short of its reported total~~ | ~~1.1~~ | ~~An endpoint claiming 100 and stopping at 50. A short read that returns successfully looks identical to a complete one~~ [superseded by D-71, and the row was never struck when the assertion changed direction. The same fixture is registered below as "Paged read the server ran out of", where a short read the server ran out of is recorded rather than thrown] | ~~`EodhdClientTests.APagedReadShortOfItsReportedTotalFailsRatherThanReturning`~~, now `EodhdClientTests.APagedReadTheServerRanOutOfIsRecordedRatherThanThrown` |
| Config date before every version | 1.13 | A resolution date earlier than every row's `set_at`. A resolver falling back to `MAX(version)` returns today's config for a historical date and passes every test that stays inside the configured range [INVARIANT 13] | `ConfigResolutionTests.ADateBeforeEveryVersionResolvesToNothingRatherThanTheNewest` |
| Bulk row dated other than requested | 1.2 | A feed returning a row for a different day than the one asked for. Writing it would file bars under a day they did not belong to, and no later stage could detect it | `PriceIngestorTests.ARowDatedOtherThanTheDateRequestedFailsTheStage` |
| Two form 4 lines identical on every attribute | 1.7 | One filing, one owner, one code, one date, one share count and two genuinely different lines, reduced from the measured collision: an option exercise reported as Common Stock acquired and as Restricted Stock Units disposed. A7's proposed key collapses them into one and the row count still looks right, which is how D-68 fails silently | `FlowIngestorTests.TwoLinesIdenticalOnEveryAttributeAreStillTwoRows` |
| Global earnings calendar with an off-exchange row | 1.8 | A `.BSE` symbol beside the US ones. Nothing in the request narrows `calendar/earnings` to an exchange, so a stage that does not narrow writes 22,286 rows across every market the provider carries and every one of them looks like a real event | `EventsIngestorTests.AnExchangeOutsideTheUniverseIsDropped` |
| Bulk event row keyed by code and exchange separately | 1.8 | `{"code":"NVDA","exchange":"US"}` where the calendar sends `NVDA.US`. A parser reading `code` alone matches nothing against the universe and writes no rows while the stage succeeds | `EventsIngestorTests.TheBulkFeedsTickerIsAssembledFromCodeAndExchange` |
| Dividend payload carrying four dates | 1.8 | Ex-date, declaration, record and payment, of which exactly one is the event and one is the announcement. The other two are the ones a reader reaches for by name | `EventsIngestorTests.ADividendKeepsTheExDateAndTheDeclarationDateAndDiscardsTheOtherTwo` |
| Six insider filings of which three count | 1.8 | One ticker with a purchase inside the window, a sale inside it, a purchase before it, a purchase filed after the run date, and an option exercise. The reference answer is hand-computed in the test, so a change to the statement that alters a number fails rather than passing with a different one | `FlowEngineTests.TheThreeMetricsReproduceTheHandComputedReference` |
| Open-market row with no dollar value | 1.8 | A sale with neither a total nor a price. Dropping it from the sum is adding zero, and zero means insiders traded and netted out | `FlowEngineTests.ATickerWithAnUnpricedOpenMarketRowCarriesNullRatherThanAnUnderstatedTotal` |
| Ticker with no ingested flow at all | 1.8 | Never through the rotation, against one with holdings but no filings and one with filings but no purchases. Three different facts that a zeroed row would render identical | `FlowEngineTests.ATickerWithNoIngestedFlowGetsNoRowRatherThanZeros` |
| Institutional report inside its filing lag | 1.8 | A report dated 2026-06-30 read on the 13th and 14th of August, either side of the forty-five day 13F deadline. Reading on `report_date` alone is INVARIANT 12's mistake arriving through the other table with the same shape | `FlowEngineTests.AnInstitutionalReportIsNotReadableUntilTheFilingLagHasPassed` |
| Paged read that stops while the endpoint offers more | 1.8, item 59 | An endpoint claiming 100 and serving nothing from page three, offering a next link while its own `offset + limit < total` arithmetic says one exists. **This changed direction at item 59 and it is the same fixture**, for the second time and the same reason as at D-71: the empty page is walked past, the server then runs out, and the 50 rows owing come back as a recorded shortfall rather than a throw. Measured against the provider on 2026-08-22, `links.next` is that arithmetic over a total the endpoint over-counts, so it cannot mean rows remain, and D-71's test for the fatal half is that asking again would fix it | `EodhdClientTests.AnEmptyPageInsideTheAddressableRangeIsWalkedPastRatherThanRefused` |
| Paged read that runs out of addressable offsets while still yielding | item 59 | An endpoint claiming 100, serving 23 on page two where every other page serves 25, and offering a successor **forever**. This is D-71's fatal half kept reachable rather than widened away: the client stops because an offset at or past `meta.total` addresses no row of a `total`-sized index, so it stops with rows still arriving and a successor still on offer, which is the client failing to ask. Bounded at `ceil(total / pageSize)` pages, which is what replaced the empty-page break as the spin guard. **Written because an unreachable throw is decoration** | `EodhdClientTests.APagedReadThatRunsOutOfAddressableOffsetsWhileStillYieldingFails` |
| Paged read the server ran out of | 1.8 | The same fixture that used to prove a throw, now proving a recorded shortfall. The endpoint stops offering a next link with rows still owing, which is the provider disagreeing with itself and cannot be recovered by asking again [D-71] | `EodhdClientTests.APagedReadTheServerRanOutOfIsRecordedRatherThanThrown` |
| Short page before the last | 1.8 | AAON.US reduced: a page that comes back short mid-sequence puts the missing rows inside the history where a trailing-90-day metric reaches them, where a short final page puts them at the oldest end [D-71] | `EodhdClientTests.AShortPageBeforeTheLastPutsTheMissingRowsInsideTheHistory` |
| Config store where the key that changed is not the highest-versioned one | sign-off | Three keys at versions 3, 1, 1, then the key at 1 moves to 2. A store-wide version taken as `MAX(version)` answers 3 both times, so two different configurations carry one stamp and the tuner pools what it exists to separate. The test asserts the version moves and, alongside it, that the maximum does not [D-72] | `ConfigResolutionTests.ChangingAKeyOtherThanTheHighestVersionedOneStillMovesTheStoreWideVersion` |
| Key seeded by a later phase, backdated as the seeder backdates | sign-off | A new key at version 1 stamped `2020-01-01`, which is what every phase's seeding does. Counting rows rather than revisions would raise the store-wide version for every date from the window start onward, and a backfill re-run would stamp a different version on identical data. The test asserts four prior dates are unmoved and, alongside it, that a row count would have moved [D-72 as amended] | `ConfigResolutionTests.SeedingAnAdditionalKeyLeavesEveryPriorDatesVersionUnchanged` |
| Stage disagreeing with its Reads cell, both ways | reconciliation | Two fabricated read sets against the real catalogue: `PriceIngestor` declaring `price_daily` where its cell is one bulk endpoint, and `FlowEngine` declaring nothing where its cell names two tables. Each direction is asserted to fire and the other to stay silent, because both would pass vacuously if the catalogue parse returned an empty map and neither failure would look like one [D-74] | `ReadDeclarationConformanceTests.BothDirectionsFailOnAStageThatDisagreesWithTheCatalogue` |
| Two for one split inside the indicator window | 2.5 | Raw close halving mid-window while the adjusted close does not move. Computed on the raw series the 200-day average reads a fifty percent decline, every screen downstream sees a name that has collapsed, and nothing errors. Volume is the same failure in the other direction, doubling on the split | `IndicatorEngineTests.ASplitInsideTheWindowDoesNotReadAsADecline` |
| Flat series with a closed-form ATR and index | 2.5 | Close 100, high 101, low 99, volume flat. Wilder's smoothing of a constant is that constant whatever the seed, so ATR is exactly 2 and the index exactly 0 without reimplementing the recursion in the test. Nine of the ten computed columns are read off four numbers | `IndicatorEngineTests.TheFlatSeriesReproducesItsClosedForm` |
| Series rising by a fixed amount every bar | 2.5 | One-sided directional movement throughout, so +DI smooths to 50, -DI to 0 and DX to 100 at every step. The second closed form, and the one that would catch a directional-movement sign error the flat series cannot see | `IndicatorEngineTests.ASteadilyRisingSeriesHasAnIndexOfOneHundred` |
| Gap inside the Wilder window | 2.5 | One bar missing a high, 40 bars back. Smoothing across a hole reports a number computed over a series that does not exist, which is a shorter window wearing the right name | `IndicatorEngineTests.AGapInsideTheWilderWindowNullsTheSmoothedColumns` |
| A new high out of a base against one out of a trend | 2.5 | The same closing price clearing the same window high, once after 60 flat bars and once after 300 rising ones. Without the range condition a name in a steady advance sets a new window high most days, the flag is permanently on, and every strong trend enters S2 twice | `IndicatorEngineTests.ABreakoutNeedsABaseBehindItAndNotJustANewHigh` |
| A name moving exactly with its benchmark | 2.6 | Relative strength is a ratio, so a name that moves with the benchmark has zero change at every horizon and zero slope, whatever either of them did. It separates a relative-strength column from a return column, which agree on every series that does not move together | `IndicatorEngineTests.MovingWithTheBenchmarkIsNoRelativeChangeAtAnyHorizon` |
| A rising name against a flat benchmark | 2.6 | Exact fractions rather than rounded figures: 399/378 - 1 at 21 dates and 399/336 - 1 at 63, with the acceleration column their difference. A horizon off by one bar fails it | `IndicatorEngineTests.AgainstAFlatBenchmarkTheChangesAreThePricesOwn` |
| A universe member with no sector | 2.6 | C01 writes null rather than a guess when the sector lookup fails, so the sector-relative column is null rather than a comparison against the whole universe wearing a column name that says sector | `IndicatorEngineTests.ANullSectorCarriesNullRatherThanAUniverseWideComparison` |
| Rotation pool name that returns no rows | sign-off | A selected ticker that writes nothing, standing for the 14 of 250 answering `404 Symbol not found`. It stays never-fetched and is offered again on the next pass, which is the residue a high-water mark would close and is asserted so it is a known property rather than a surprise | `FlowIngestorTests.ANameThatReturnedNoRowsIsOfferedAgainRatherThanSkipped` |
| Twenty bars with an interpolating median | 2.5 | Dollar volumes 1,000 through 20,000, so `percentile_cont` returns 10,500, the mean of the tenth and eleventh, where `percentile_disc` would return 10,000. It pins the choice between them as a definition rather than a detail, and it is the first thing that exercised the shared expression through the reader that reads its type back | `DollarVolumeTests.TheColumnC08WritesIsTheNumberC01SFloorReads` |
| Eight quarters reported twice over | 2.7 | Four recent quarters identical and four a year older identical, so every trailing twelve month sum is four times one number and every four-quarter change is a difference of two exact ratios. Ten of the twelve computed columns are read off it without running anything | `ValuationEngineTests.TheSteadyCompanyReproducesItsClosedForm` |
| Capital expenditure against free cash flow | 2.7 | Cash from operating 400 and capital expenditure 100 over the trailing year. Subtracted as a positive magnitude free cash flow is 300; read as a negative outflow and added it is 500, and the yield is 0.25 rather than 0.15. Both look like yields, which is why D-79's sign had to be measured against the store rather than argued | `ValuationEngineTests.CapitalExpenditureIsSubtractedAsAPositiveMagnitude` |
| A quarter filed after the date being computed | 2.7 | The newest period filed the day after the run date and carrying an EBIT twenty times the others, so a point-in-time leak is EV/EBIT under 0.5 against a readable 12.5 rather than a rounding difference. The same fixture with the filing date brought forward moves, so the assertion is about the filter [INVARIANT 12] | `ValuationEngineTests.AQuarterFiledAfterTheDateIsNotReadable` |
| A quarter with no effective filing date | 2.7 | D-62 made the column nullable because a ticker with no clean gap has no date to substitute from. Null there means no usable filing date and none derivable, so the period is excluded rather than assumed public | `ValuationEngineTests.AQuarterWithNoEffectiveFilingDateIsNotReadable` |
| Three quarters where four are needed | 2.7 | Every trailing twelve month metric null and the two that need only the latest quarter still computed, so the nulls are the rule rather than an empty fixture. Summing three and calling it a year understates every ratio built on it, on exactly the names whose filing history is thinnest | `ValuationEngineTests.ThreeQuartersIsNotAYear` |
| Filings dated a year after their period end | 2.7 | Seven and a half years of quarters against sixty month-end sample dates. A sample five years back must see fewer filings than the run date does, which is what fails if the own-history column resolves its filings as of the run date rather than as of each sample | `ValuationEngineTests.EachOwnHistorySampleResolvesFilingsAsOfItsOwnDate` |
| Own history at and one below the configured floor | 2.7 | Twenty-four identical sample points at a close of 1 against a run-date close of 2, so the median is 7.5 and the column is exactly 2/3. Twenty-three of the same points is null, which is the floor rather than a computation over less | `ValuationEngineTests.OwnHistoryIsAMedianAndIsNullBelowTheConfiguredFloor` |
| Ninety-day baseline half at two articles and half absent | 2.8 | Forty-five days carrying a row and forty-five carrying none. Zero-filled the mean is exactly 1 and the population deviation exactly 1; unfilled the mean is 2, the deviation 0 and the column null. The fixture separates the two readings rather than agreeing with both | `SentimentEngineTests.TheZScoreIsAgainstTheTickersOwnZeroFilledBaseline` |
| Two baselines of the same shape a hundred-fold apart | 2.8 | A thinly covered name and a widely covered one scoring identically, which no cross-sectional count could do. A cross-sectional article count measures analyst coverage, which is a size proxy [D-12] | `SentimentEngineTests.TheZScoreIsNotCrossSectional` |
| A row every third day, scored throughout | 2.8 | Both tone windows are means over the rows that exist, so both are 0.5 and the delta is exactly zero. Zero-filling the score would make the delta positive on a name whose tone never moved, in proportion to how thinly covered it is | `SentimentEngineTests.AThinlyCoveredNameHasNoToneOnTheDaysItHasNoNews` |
| Nineteen days inside a baseline floor of twenty | 2.8 | Rows on the date itself, so the tone windows have something to average and are null anyway. One more day inside the baseline and all three become computable, so the nulls are the floor gating all three rather than the fixture being empty | `SentimentEngineTests.BelowTheBaselineFloorEveryMetricIsNull` |
| A stored day carrying no count, and one carrying no tone | 2.8 | An absent day is a measurement and a present row with nothing in it is not. The first nulls the z-score; the second contributes nothing to either mean and leaves the count untouched | `SentimentEngineTests.ARowWithNoCountNullsTheZScoreRatherThanCountingAsZero`, `.ARowWithNoScoreContributesNothingRatherThanZero` |
| Seeded universe of three buckets and four cells | 2.10 | Sixteen values in a twenty-member cell put the highest at 100; against its bucket of twenty-one it would be 75, and with the four nulls in the denominator 78.9. Three numbers, three readings, one fixture. The thin cell of five falls to the bucket at 80 where its own cell would say 0, and the ten-member bucket carries null rather than a cross-bucket rank | `PercentileEngineTests.ACellThatClearsTheFloorIsRankedInsideItsSector`, `.AThinCellFallsBackToTheSizeBucketAlone`, `.AThinBucketCarriesNullRatherThanACrossBucketRank` |
| Sixteen members with no sector inside a bucket of thirty-one | 2.10 | Postgres groups nulls together in a `PARTITION BY`, so without the rule they would form a cell and be ranked against each other under a column that says sector. That cell puts the highest at 100; the bucket puts it at 50 | `PercentileEngineTests.ANullSectorIsRankedInItsBucketRatherThanAgainstOtherNullSectors` |
| Staging table widened to the full column set | 2.10 | A one-line edit that looks like completeness, keeps the registry green because the operation is still `Insert`, and blanks every percentile on the next run of a date already ranked. It is what makes the survival assertion beside it mean something [D-77] | `PercentileSplitTests.WideningTheStagingTableToTheFullColumnSetBlanksThem` |
| One insider purchase behind a re-run of C34 | 2.10 | `flow_daily` takes its column ownership from its own `DO UPDATE SET` list rather than from a staging table, which is a different mechanism and so is run rather than reasoned about from the other three | `PercentileSplitTests.ReRunningTheFlowEngineLeavesEveryPercentileByteIdentical` |
| A statement with a declared column renamed out of it | 2.11 | A real `UPDATE` mutilated by replacing one declared column name. A substring search over a long statement is exactly the check that passes on everything, so it is shown failing on the case it exists for | `StatementColumnConformanceTests.TheCheckFailsWhenADeclaredColumnLeavesTheStatement` |
| A company reporting cash with no cash-and-equivalents line | 2 sign-off | `NFLX.US`'s own quarter ending 2026-06-30: no parts line, 9,099,232,000 of `cash` and 28,678,000 of short-term investments. The zero-coalesce wrote the investments alone, two orders of magnitude out, under a column that says cash on hand. Both the right answer and the old one are asserted, so a reversion fails rather than passing differently [D-81] | `ValuationEngineTests.TheCashLineStandsInForAnAbsentCashAndEquivalentsLine` |
| Neither cash line present | 2 sign-off | Short-term investments alone, which D-81 nulls rather than reporting as the whole. The one place the rule change removes a value, and it is 5 names | `ValuationEngineTests.BothCashLinesAbsentIsNullRatherThanTheInvestmentsAlone` |
| Eight identical quarters and one price, run through C09 | 2 sign-off | The first fixture to reach `valuation_daily` through the stage rather than through `Compute`. One row carries a `real` ratio, a `numeric` level and a `real[]` written null, which is three wire formats in one write. C09's write path had executed exactly once, on the night of 2.12 | `StageWritePathTests.TheValuationEngineWritesThroughItsOwnPathAndTheRowReadsBack` |
| Forty days of coverage with a moving article count | 2 sign-off | Enough to clear the baseline floor with a non-zero deviation, so all three columns come back valued rather than null. A stage that wrote three nulls would read back identically to one that had not run, which is what this rules out. C35's write path had also executed exactly once | `StageWritePathTests.TheSentimentEngineWritesThroughItsOwnPathAndTheRowReadsBack` |
| Market context on a date with no universe and no prices | 2 sign-off | An empty sector document opens with the same brace that binary COPY reads as a jsonb format version, so the 2.9 defect needs no seeded universe to catch. What it needs is the stage's own call: reverting `MarketContextEngine` to `WriteAsync` left all 221 tests passing before this fixture existed | `StageWritePathTests.TheMarketContextEngineWritesJsonbThroughItsOwnPath` |
| Six pool members and a rotation of two | 0006 | Two runs selecting disjoint pairs, a third selecting neither, and a fourth cycling back to the first. The defect wrote a healthy row count on every one of those runs while covering the same names, so the selection is the observable and the row count is not | `FundamentalsRotationTests.TwoConsecutiveRunsOverAnUnchangedPoolSelectDisjointSets`, `.TheRotationKeepsCyclingAfterCoverageCompletes` |
| A fundamentals payload in the bare-string shape | 0006 | The response for a ticker the endpoint carries no financials for, which writes no row. Keyed on rows written its freshness never moves and it holds the head of the rotation for ever, which is why the record is of the attempt. Also asserts that a later empty attempt does not erase an earlier yield date | `FundamentalsRotationTests.ATickerWhoseFetchReturnsNoRowsDoesNotReappearInTheNextRun`, `.AnEmptyAttemptDoesNotEraseAnEarlierYieldDate` |
| One date run twice through the rotation | 0006 | Two runs over the same date selecting the same names. The rotation advances between dates and never between runs, which is what the strictly-before read buys and what an attempt timestamp would have cost [`CLAUDE.md` §6] | `FundamentalsRotationTests.TwoRunsOverTheSameDateSelectTheSameNames` |
| The `/api/user` payload as it was returned | 3.4 | Twelve keys with `apiRequests` 90,518, `apiRequestsDate` 2026-08-11 and `dailyRateLimit` 100,000, captured at 3.1 with the identifying fields redacted. The gate reads three of them and the parse is asserted against what the provider sent rather than against what it was assumed to send | `AllowanceRuleTests.TheUserPayloadParsesToTheThreeFieldsTheGateNeeds`, `.AMissingOrUnreadableFieldIsAHaltRatherThanADefault` |
| An allowance reading stamped with a different provider day | 3.4 | 90,518 used stamped 2026-08-11, read against a provider date of 2026-08-12, which is the rollover 3.1 fell across mid-sweep. A gate that subtracted across it concluded it had spent negative units, which is what the sweep's own bracket printed. The reading is unusable in either direction and takes a third verdict rather than being read as a reset or as a spend | `AllowanceRuleTests.AReadingStampedWithADifferentDayIsStaleRatherThanSpentOrClear`, `.StaleAndExhaustedAreDifferentObservationsAndSayDifferentThings`, `BackfillRunTests.AStaleAllowanceReadingHaltsBeforeAnythingIsSpent` |
| A sweep whose allowance runs out mid-pool | 3.4 | Fifteen tickers at 10 units against 100 left above the reserve, with the stub spending as the sweep works. Ten fit and the eleventh does not, so the halt is the loop rather than a single comparison: the ten written are kept and the position recorded is where the next run resumes rather than the last unit done | `BackfillRunTests.TheGateHaltsTheSweepAndKeepsTheRowsAlreadyWritten`, `.TheSameSweepCompletesWhenTheAllowanceHasRoom` |
| A 402 and a 429 reaching the paging client | 3.4 | An allowance error in flight against a server that ran out of pages, in one file so the distinction is read rather than inferred. The first fails the stage; the second is a recorded shortfall [D-71]. Absorbing the first as the second reports a complete sweep over a partial one, and the two are one absorbed observation apart | `AllowanceErrorsAreNotShortfallsTests.A402ReachingThePagingClientFailsTheStage`, `.A429ReachingThePagingClientFailsTheStage`, `.AServerThatRanOutOfPagesIsAShortfallRatherThanAFailure` |
| Eight universe members and a rotation of four, cycled past coverage | 3.5 | C05's counterpart of the 0006 fixture. Two passes cover the pool and the third and fourth keep cycling, with the oldest attempt in the selection advancing a day each time. Keyed on rows in `insider_transaction` the third pass would repeat the first for ever and the row count would not say so [D-95] | `FlowIngestorTests.TheRotationKeepsCyclingAfterCoverageCompletes`, `.TwoConsecutivePassesOverAnUnchangedUniverseSelectDisjointHeads` |
| A universe name that answers 404 | 3.5 | One of the 14 in 250, standing for the whole group. It writes no rows, so under the old ordering it stayed never-fetched and was re-asked every run at 10 units a call [3.1]. With the attempt recorded it drops out of the next pass and returns on the one after, at the position its staleness earns rather than at the head. The fixture that used to assert the defect as a known property now asserts it closed [D-95] | `FlowIngestorTests.A404NameIsReOfferedOnALaterPassRatherThanHeldAtTheHead` |
| An unfiltered `Holders` block, captured live | D-98 | `CCS.US`'s whole `Holders` block out of the unfiltered `fundamentals/{t}` payload, 6,547 bytes at `docs/evidence/phase-3/holders-ccs-unfiltered-20260812.json`. Registered because a fixture built from the **filtered** response would prove nothing about the unfiltered one: the claim under test is that the second is not a truncated head of the first, which providers do. The live comparison behind it is twenty entries both ways on CCS.US and NVDA.US, agreeing row for row [`holders-filtered-vs-unfiltered-20260812.txt`]. **What the test over it can claim is narrower than the fixture's own reason for existing, and it says so.** One capture is one response, so comparing it against itself cannot re-prove the provider half; what it proves is that our parse reads the unfiltered nesting, the captured block and the filtered root identically and takes all twenty rows from each. The block also carries `Funds` beside `Institutions`, twenty more names and none of them institutions, so taking the block whole rather than the named member is a failure the same fixture catches | `InstitutionalHoldersTests.TheNestedShapeAndTheRootShapeParseToTheSameRowsInTheSameOrder`, `.EveryShapeOfTheCapturedBlockYieldsAllTwentyEntries`, `.TheFundsBlockBesideItIsNotRead`, `.RowsAreSortedByReportDateThenHolderName` |
| Two earnings entries resolving to one period end | 3.7 | Four payloads: different report dates in each order, both absent, and one dated against one undated. Taking the period end from the entry's `date` gives up the uniqueness the object key had, so two entries reach one COPY with the same conflict target and Postgres raises `ON CONFLICT DO UPDATE command cannot affect row a second time`, which is a stage failure mid-sweep after the units before it are spent. The later report date wins, the last in document order wins on a tie, and a dated entry beats an undated one whichever came first | `EarningsHistoryTests.TwoEntriesForOnePeriodEndWriteOneRowAndTheLaterReportDateWins`, `.TheLaterReportDateWinsWhicheverOrderTheyArriveIn`, `.TwoUndatedEntriesForOnePeriodEndWriteOneRowWithoutThrowing`, `.ADatedEntryBeatsAnUndatedOneWhicheverCameFirst` |
| A failed range row above a halted one | 3.6 | A halt reaching 2022-06-03 inside a range ending 2022-06-30, then a run over the same range that throws. The report takes the highest `run_date` unfiltered, so a failed row stamped with its range end would carry it past everything the failure skipped. Stamped with its range start, the maximum falls back to the halted run's reached date. Nothing resumes on this since 0010 and the fixture is kept: the row is what an operator reads before spending another day, and a date that overstates what happened is as misleading to a person as it was to a resume | `BackfillRunTests.AFailedRunAboveAHaltedOneDoesNotCarryResumptionPastTheHalt`, `.AFailedRunReportsItsRangeStartAndNoPosition` |
| Twenty-two admitted names across two symbol lists, gated mid-sweep | 3.6 | Twenty live common stocks and two delisted, plus an ETF and two funds D-4 excludes, against an allowance leaving three units above the reserve. The first chunk of eight loads and the second is refused, so the halt lands at a chunk boundary rather than racily. Eight tickers carry an attempt row and `L07.US`, the ninth ordinally, does not, so the next run makes the pool less eight calls rather than the whole pool. Running it a third time makes none, which is the case that spent a day of allowance on 2026-08-13 re-fetching a finished load. The fixture range starts at 2019-06-03 rather than at `backfill.window_start`, so a fixture attempt can never be read as a real one [D-99] | `PriceBackfillTests.TheSweepHaltsAtAChunkBoundaryAndOnlyTheDispatchedTickersCarryAnAttempt`, `.TheNextRunDispatchesOnlyTheTickersWithNoAttemptForThisRange`, `.ACompletedSweepReInvokedOverTheSameRangeDispatchesNothing`, `.ThePoolIsTheTwoAdmittedListsAndTheReferenceSeries` |
| The benchmark in the fetch pool and no other ETF in it | D-104 | The same two symbol lists, asserted for what they do not decide: `SPY.US` is in the pool although neither list carries it, and `LETF.US` is out although it is the same instrument type. **This is the assertion whose absence cost four of five and a half years of two columns.** C08 and C10 both read `SPY.US` and C02's pool was admitted common stock, so no sweep ever asked for it: 265 bars against a 1,584-date window, no attempt row at all, 2,690,981 of 4,143,273 indicator rows with null relative strength and 0 of 1,584 regime labels at `risk_off`. Nothing errored, because both components null correctly on a short window. The pool size is stated once in the fixture rather than at each assertion, so a second reference series moves all eight together | `PriceBackfillTests.ThePoolIsTheTwoAdmittedListsAndTheReferenceSeries` |
| A price ticker that answers 404 | D-99 | `L05.US` of twenty-two, standing for every name the price endpoint does not carry. It writes no bars, so a resumption keyed on presence in `price_daily` would call it never fetched and re-ask it for ever, which is 0008's fourteen-of-250 defect one table over. It carries an attempt row with a null `last_yield_date`, a name that did yield carries the date, and the next run makes no calls at all. That pair is what makes null a fact rather than a column nobody writes | `PriceBackfillTests.ATickerThatYieldsNothingIsAttemptedOnceAndNotAskedAgain` |
| Store matrix with a store row removed | store matrix | §16 with `security_daily`'s row cut out of a copy held in memory. The schema-to-matrix direction reports that store by name and the count drops by exactly one, and the unmutated document passes, so the failure is the removal rather than the mutation having broken the parse. The four stores that drifted out of this table did so silently and this is what makes the check known to fire | `StoreMatrixConformanceTests.RemovingAStoreFromTheMatrixFailsTheCheckAndNamesThatStore` |
| Store matrix naming a store nobody declared, with and without the marker | store matrix | An invented `srl_no_such_store` row inserted into a copy. Unmarked it is reported; carrying `NOT YET IN SCHEMA` it is not, which is the whole of what the marker buys. The staleness half is asserted separately, so a marker cannot outlive the condition it records | `StoreMatrixConformanceTests.AStoreTheSchemaDoesNotDeclareIsReportedUnlessItCarriesTheMarker`, `.AStoreMarkedNotYetInSchemaIsGenuinelyAbsentFromIt` |
| One pool through both rotations | 3.5 | The same pool, attempts and universe set through `RotationSelection` twice. What is shared is the function and never the table, because two components writing one table is two claims on one triple [INVARIANT 10]. C05's ordering was copied from C03's by hand at 1.7 and kept the defect after C03's was fixed, which is the drift this removes | `FlowIngestorTests.BothRotationsGetTheSameAnswerFromTheSameInputs` |
| A range opening before the seed instant | 3.4 | 2019-12-30 against a `SeedInstant` of 2020-01-01, so every date in the range resolves to no config version at all. It fails the run rather than computing against a configuration that resolved to nothing, which is the failure that would otherwise be silent: today's resolution still works [D-72, D-94] | `BackfillRunTests.AWindowStartBeforeTheSeedInstantFailsWithTheConfigVersionError` |
| Two holder entries resolving to one key | item 21 | Six payloads: the same institution twice at one report date in each order, tied on shares, one known share count against an absent one in each order, and both absent. The entry key is a position, so the provider's uniqueness is over positions and not over holders, and two entries naming one institution at one report date reach one COPY under one conflict target. The larger current share count wins, the first in document order wins on a tie, and a known count beats an absent one whichever came first. **Summing is what the fixture rules out**: 100 and 900 write 900 and never 1,000, because adding them writes a number the provider did not send | `InstitutionalHoldersTests.TwoEntriesForOneHolderAndReportDateWriteOneRowAndTheLargerHoldingWins`, `.TheLargerHoldingWinsWhicheverOrderTheyArriveIn`, `.TwoEntriesTiedOnSharesKeepTheFirstInDocumentOrder`, `.AKnownShareCountBeatsAnAbsentOneWhicheverCameFirst`, `.TwoEntriesWithNoShareCountAtAllWriteOneRowWithoutThrowing`, `.TheSameHolderAtTwoReportDatesIsTwoRows` |
| The candidate pool, old statement against new, on the loaded store | item 24 | Not a fixture in the test suite and recorded here because it is the evidence a rewrite rests on. `price_daily` at 78,087,416 rows, the old statement's 7,320 tickers dumped once, the shipped statement extracted verbatim out of `FundamentalsIngestor.cs` and compared element by element: 0 missing, 0 extra, orders equal, 169.9s against 11s. **The sixty-day slice that was expected to be the fix is in the same record as rejected**, at 4,834 tickers and no faster. A rewrite whose output is only spot-checked is a redefinition nobody noticed, and this is what makes it not one | `PROGRESS.md`, 2026-08-13. No test asserts it: the comparison needs a loaded store and CI has an empty one |
| Four hundred tickers through the real write path | item 23 | A symbol list of 400 live common stocks swept end to end, with `pg_stat_database.sessions` read either side. **7 physical connection opens at a worker count of 8**, which is what refutes the hypothesis that `BulkUpsertAsync` opening a connection per call defeats pooling and pays a TCP, TLS and SASL round trip per ticker. Asserted as an upper bound rather than an equality, because above the pool's floor the number depends on how workers interleave; what the bound rules out is one open per ticker | `PriceBackfillTests.ConnectionsArePooledSoAFewHundredTickersOpenAboutTheWorkerCount` |
| A middle ticker that throws inside the parallel body | D-99 | `L0200.US` of 400 answering with an object where the series is an array, which `LoadSeriesAsync` calls a shape change and throws on. It stands for every way a sweep stops without saying so, a killed process included. The ticker that threw carries no attempt row, because attempts are committed after the chunk that produced them, and the resume dispatches **exactly** the complement of the attempt set. Asserted as set equality rather than as a count, since a count is satisfied by re-dispatching the wrong tickers, and afterwards every pool member carries a row. This replaced the frontier fixture, which asserted the same throw produced a position within one chunk of the failure: true, and produced on one of three real failures | `PriceBackfillTests.AFailedSweepResumesOverExactlyTheTickersWithNoAttemptRow` |
| Six pool members and a sweep with room for three | 3.7 | `SRLFRG*`, priced and liquid above D-4's floors with 250 sessions each, driven through the real `UnitAllowance` over a double that bills what it serves at one unit a symbol list and ten a `fundamentals/{t}`. Seeded at 49,960 spent, the gate first sees 38 above the reserve and stops the fourth call, so three land and three do not. **The observable is which tickers were asked for**: a restart writes a healthy row count over names it already had, which is how the frozen rotation survived three runs and a sign-off. The complement is asserted in both directions rather than counted. **The delisted list carries one admitted name with no bars** rather than being empty, `SymbolList` treating a list with no `Common Stock` as a shape change, so the half is exercised and adds nothing | `FundamentalsRangeTests.AHaltedSweepResumesOverExactlyTheTickersWithNoAttemptRow`, `.ACompletedSweepReInvokedOverTheSameRangeDispatchesNothing`, `.ASweepAskedForADifferentRangeEndStartsOver` |
| The per-ticker distribution shapes, and an earnings row that cannot arrive | 3.10 | `div/{t}`'s eight fields as 3.1 measured them on `SPY.US` and a `splits/{t}` payload, parsed through the range path. **The one structural difference from the bulk form is that the ticker is not in the payload**: the bulk feeds send `code` and `exchange` separately, which 1.8's fixture exists for, and these send neither because the ticker is what was asked for, so a parser reading `code` returns nothing while the stage succeeds. A dividend keeps its ex-date and its declaration date and discards `recordDate` and `paymentDate`; a split keeps its effective date and carries no announcement, null meaning unknown rather than simultaneous. An empty array is an ordinary name rather than a fault, measured at 3.1 on `SPY.US` itself, which is why the sweep resumes on an attempt record. **The last case feeds the range pass a payload carrying `report_date`** and asserts no earnings row comes back whichever event type is asked for, because the absence is 3.10's decision and an omission and a decision look identical in an empty column | `EventsRangeTests.ADividendKeepsItsExDateAndItsDeclarationDateAndDiscardsTheOtherTwo`, `.ASplitCarriesItsEffectiveDateAndNoAnnouncement`, `.TheTickerComesFromTheRequestBecauseThePayloadDoesNotCarryIt`, `.ATickerWithNoDistributionsParsesToNothingRatherThanFailing`, `.ARowWithNoDateIsDroppedRatherThanDefaulted`, `.TheRangePassCannotProduceAnEarningsRowWhateverItIsServed` |
| A delisted name inside the window, one before it, and one with no bars | 3.8 | `SRLSNT*`, three names on a handler-served delisted symbol list against a seeded `price_daily`: one trading from the window start, one whose bars stop the day before it, and one with none. **The middle case is what keeps the sweep affordable**, 15,749 of the 32,611 admitted delisted names having stopped trading before the window start, so a pool taking the list whole would spend half as much again buying days no evaluation date reaches. The list is the fixture's own, so the intersection is bounded by the fixture whatever `price_daily` holds and the assertions hold on a populated developer database as well as an empty CI one. The config store passed in **refuses every key**, which asserts that D-101's pool rule takes no configuration rather than merely not reading any today | `SentimentRangeTests.TheDelistedHalfIsTheAdmittedNamesCarryingABarInsideTheWindow`, `.ANameThatStoppedTradingBeforeTheWindowIsNotInThePool`, `.ANameWithNoBarsAtAllIsNotInThePool` |
| A paged walk stopped by its caller, against one the server ran short on | 3.9 | Three pages of two rows claiming six, walked with a gate that allows one page, against one page of two rows claiming five with no next link. **The two look identical from a row count and are opposite facts.** The gated walk reports `StoppedByGate`, two rows of a claimed six, and a shortfall of **zero**: the four that did not arrive were never asked for, and counting them would put a spending decision into the record as a provider defect. The short walk reports a shortfall of three at `Final`. A third case refuses before the first page and asserts no page was served, which is what stops a sweep with nothing left spending ten units to discover that | `FlowRangeTests.AGatedWalkStopsWithoutRecordingAShortfall`, `.AnUngatedWalkThatRunsOutOfPagesShortIsAShortfallWithAPosition`, `.AGateThatRefusesImmediatelyCostsNoPage` |
| A gated halt and a shortfall rendered side by side | 3.9 | The two sentences a halted sweep's run log carries, asserted against each other rather than each alone: the halt names its ticker, says the next run walks it from its first page, and denies the other reading in the words a reader would search for; the shortfall line counts one ticker and does not mention the gated one. **Neither may borrow the other's vocabulary**, so the halt is asserted not to contain "under-delivered" at all. Composed rather than swept, C05's pool being the universe, which on the developer database the suite then ran against was the live one ~~[open items 10 and 26]~~ [item 26, closed at 3.13; the sweep itself is still owed at item 33] | `FlowRangeTests.TheGatedHaltAndTheShortfallReadAsTwoDifferentThings`, `.NoShortfallIsStatedRatherThanOmitted` |
| A blackholed address, a name that does not resolve, and two hosts that answer | item 23, D-100 | `203.0.113.1` is TEST-NET-3 and is not routed, so a one-second connect timeout ends the attempt and it retries twice and reports the count. `srl-no-such-host.invalid` at a fifteen-second timeout fails in the resolver and retries zero times. A statement failing on an open connection and ~~a refused login~~ **a database that cannot exist** [D-103] each retry zero times as well. **The pair is the point**: one fixture proves the transient half and one the permanent half, and before D-100 a single fixture claimed both while exercising whichever the local resolver's speed selected. The blackholed case asserts the count and deliberately not the exception type, a connect timeout arriving as a bare `TimeoutException`, an `NpgsqlException` wrapping one or a `SocketException` carrying `TimedOut` depending on where in the handshake it lands. **The trigger and the name are separated at D-103**: three of the four are named for the property they pin and each carries its trigger, and the reason it is guaranteed, in its own doc comment | `StageDataGuardTests.ATransientConnectFailureIsRetriedAndTheCountIsReported`, `.APermanentConnectFailureIsNotRetried`, `.AStatementThatFailsOnAnOpenConnectionIsNotRetried`, `.AConnectionTheServerRefusesIsNotRetried` |
| A reset socket, an unresolvable one, and four statuses at the client | D-100 | A stub handler raising `HttpRequestException` over a chosen `SocketError`, and one serving a chosen status for the first N calls. `ConnectionReset` retries and the second attempt succeeds; `HostNotFound` is called once; `429` retries and succeeds; `402` and `404` are each called once. **It is the same rule as the row above with this layer's own vocabulary**, and the fixture exists because the fault it is written for cost a 128-minute sweep and about 13,500 tickers' work to one reset socket in roughly 50,000 requests | `EodhdClientTests.AResetConnectionIsRetriedAndTheAttemptAfterItSucceeds`, `.AHostThatDoesNotResolveIsNotRetried`, `.AThrottledResponseIsRetried`, `.AnExhaustedAllowanceIsNotRetried`, `.ATickerTheProviderDoesNotCarryIsNotRetried` |
| A halt over one range and a sweep asking for another | D-99 | A halt recorded over `2021-01-04..2021-01-08`, then a sweep over `2021-01-04..2026-08-12`, which is the shape of the fixture row that stood in front of the first real 3.6 sweep. **The assertion is now that it runs.** The refusal this fixture used to prove was the right answer to a position belonging to a range and is the wrong one to a record keyed on the range start, which a sweep re-invoked the next morning reads whatever `to` resolves to. The row beside it, a range row `BackfillRun.Describe` could not have written, stops nothing either: nothing parses the line for a range any more, so a line it cannot read is a line it does not read | `BackfillRunTests.AHaltedRowOverADifferentRangeDoesNotStopTheNextRun`, `.ARangeRowNothingCouldHaveWrittenStopsNothing` |
| A sweep's own rows, cleared and counted | item 22, D-99 | The gated 22-ticker sweep writes one range row and eight attempt rows, both counts are asserted, the clear runs and both are asserted at zero. A test cannot assert what its own `DisposeAsync` does after it, so this asserts the thing `DisposeAsync` calls. **The attempt half is the one that matters now**: a run log row left behind is read by an operator, and an attempt row left behind makes the next sweep skip a ticker. The clear names the fixture's own tickers rather than a date or a prefix, because deleting by `last_attempted_date` would take out a real sweep's whole record and `LIKE 'L%'` would take out real tickers | `PriceBackfillTests.TheHarnessLeavesNoRangeRowAndNoAttemptRowBehind` |
| Sixteen names in one bucket and two thin sectors, ranked over five dates | 3.15 | Sixteen `indicator_daily` rows per date in one size bucket, split eight and eight across two sectors, so **every sector cell is under `percentile.cell_min_members` and the bucket clears it**: the fallback fires on every metric and the range's own run log line has to count it per metric, summed over the range rather than reported for one date. **The ranking order rotates with the date**, and that is the correction rather than the design: the first version moved the values with the date while keeping their order, which ranks identically on every date, and the distinctness assertion caught it. A percentile is a rank, so a fixture has to permute the order and not merely the numbers. **Known to fire**: ranking every date at the range end was run and fails the anchor | `PercentileSeamTests.TheRangePathRanksEveryDateTheSameWayTheNightlyPathDoes`, `.TheThinSectorFallbackFiresAndTheRangeLineCountsItPerMetric` |
| A ticker's whole sentiment history against one ninety-day window per date | 3.14 | Three members with 150 days of `sentiment_daily` behind a five-day range, counts and scores both moving. Each date is computed by the nightly stage alone and then all five by one range execution, and the three metric columns are compared per date. **The range path passes the whole history unsliced**, which is safe only because `Compute` derives all three of its windows from the date and then reads a dictionary; that is asserted separately without a database, by padding a history with extreme values twenty days before the baseline and twenty days after the date and requiring the identical answer. The second half is the one that would be silent: a future day reaching the seven-day mean makes a backfilled sentiment level prescient. **Known to fire**: starving the range read's baseline by thirty days was run and fails the anchor | `SentimentRangeSeamTests.TheRangePathWritesTheSameRowsAsTheNightlyPathOnEveryDateOfARange`, `.ComputeIgnoresDaysOutsideItsOwnWindows` |
| A purchase filed mid-range, and one older than a shortened window | 3.14 | Two names, three open-market purchases and two institutional report dates. One purchase is traded ten days before the range and **filed on its third date**, so a path windowing on `transaction_date` alone has it from the first; one is traded seventy days back, inside `FlowEngine.WindowDays` and outside any shorter window. **The second exists because the fixture did not fire without it.** Every trade in the first version fell within forty-five days, so halving the range path's window changed nothing and the seam test passed against a mutation. It now fails against it | `FlowRangeSeamTests.TheRangePathWritesTheSameRowsAsTheNightlyPathOnEveryDateOfARange`, `.NeitherPathSeesAFilingBeforeItWasFiled` |
| A breadth that differs on every date of the range | 3.14 | Six universe members, each crossing above its 200-day average on a different date, so the count above runs 0, 1, 2, 3, 4 of six across five dates and no two dates share a breadth. The benchmark carries `market.breadth_ma_days` bars behind the range so the regime label is a decision rather than the `mixed` fallback. **The distinctness is asserted as well as the equality**, and that is not decoration: the first version of this fixture wrote a constant `dist_200dma` per ticker, every date had the same breadth, and a range path computing one date's breadth and writing it to all five passed. Both were run | `MarketContextRangeSeamTests.TheRangePathWritesTheSameRowAsTheNightlyPathOnEveryDateOfARange` |
| One date computed twice, and the bar count that decides its window | item 33, 3.13 | Three names at 420 daily bars with **fifteen bars seeded past the date at a price 500 above the body**, a benchmark series and one membership epoch. The nightly stage runs the date, the rows are snapshotted, the table is cleared, and the range path runs the same date through `BackfillRun`; all fifteen metric columns are compared. **The output alone would pass with both paths wrong the same way**, so the depth is pinned separately at `IndicatorEngine.RequiredBars`: exactly 272 bars at or before the date gives `dist_52w_high_20d_change` a value and 271 gives it null, on both paths, and that number comes from the fixture's bar count rather than from either path. **Known to fire**: slicing one bar shallower was run and fails both tests. The window rule itself is asserted without a database beside it, both ends and the two degenerate cases | `IndicatorSeamTests.TheRangePathWritesTheSameRowsAsTheNightlyPathForOneDate`, `.TheBindingColumnTurnsNullAtTheSameBarCountOnBothPaths`, `IndicatorWindowTests.TheWindowEndsOnTheDateAndIsExactlyAsDeepAsAsked`, `.NoBarAfterTheDateEntersTheWindow`, `.AHistoryShallowerThanTheWindowGivesEverythingItHas`, `.ADateBeforeTheFirstBarGivesNothing`, `.ADateWithNoBarTakesTheLastOneBeforeIt` |
| A range spanning a filing date, computed twice | item 33, 3.13 | Two names, five and a half years of daily closes, and eighteen quarters whose effective filing dates sit forty-five days behind their period ends, so the quarter ending 2020-03-31 becomes public on 2020-05-15. Every date of the range is computed by the nightly stage one at a time and then by one range execution, and the rows are compared per date. **A single-date range proved nothing and that was found by mutation rather than by reading**: `RangeFilingsAsync` already narrows on the range end, so on a range whose only date is that end the per-date filter is redundant. **What the mutations established is recorded rather than assumed.** Replacing the range path's filing-date filter with `period_end` fails neither test, because `Compute` applies the point-in-time rule itself and both paths reach it, which is INVARIANT 12 living in one place. Widening `MonthEnds.At` from "strictly before" to "at or before" fails the anchor, which is the half genuinely duplicated: the nightly path reads month ends from a statement and the range path derives them in memory | `ValuationSeamTests.TheRangePathWritesTheSameRowsAsTheNightlyPathOnEveryDateOfARange`, `.NeitherPathSeesAQuarterBeforeItsFilingDate` |
| Sixty-one pool members and a sweep with room for one batch | item 33, 3.8 | `SRLSWP001..060` as `security_daily` members plus `SRLSWPGONE.US`, delisted with bars in the window, driven through the real `UnitAllowance` over a double billing five units a ticker and one a symbol list. Seeded at 49,700 spent, the first gate sees 299 above the reserve and a batch of fifty at 250 fits; the second sees 49 against a batch of eleven at 55 and stops. **C04's resumption asserted through C04**, D-99's asymmetry meaning C02's proof is not evidence about it. **Nothing asserts a pool**: three other fixtures seed `security_daily` at 2000-01-01, so the live half carries names this fixture does not own, and what is asserted instead is that no name is dispatched twice, that the fixture's own complement is dispatched both directions, and that a third run asks for nothing. A silent ticker returns no days, takes an attempt row and is not re-asked, which is D-12's thinly covered name | `SentimentSweepTests.AHaltedSweepResumesOverExactlyTheTickersWithNoAttemptRow`, `.ACompletedSweepReInvokedOverTheSameRangeDispatchesNothing`, `.ATickerThatReturnsNoDaysIsAttemptedOnceAndNotAskedAgain` |
| A walk the gate stops mid-sweep, and the ticker it stopped on | item 33, 3.9 | `SRLFLW001..008` as universe members against a handler serving one complete `form4` page a ticker at ten units. Seeded at 49,955 spent, four walks fit and the fifth is refused **before its first page**. **The property is the one only an end-to-end run shows**: the gated ticker takes no attempt row and is walked again by the next run, where the four that completed are not. The ticker is read out of the halt sentence rather than predicted, so what is asserted is the line an operator acts on. A 404 ticker takes an attempt row with a null yield date and is not re-asked, which is 0008's fourteen-of-250 defect on the sweep path. The page is complete and `meta.total` agrees with it, deliberately: a short page is D-71's shortfall, which is the other observation and is asserted separately | `FlowSweepTests.AGatedTickerTakesNoAttemptRowAndIsWalkedAgainByTheNextRun`, `.ACompletedSweepReInvokedOverTheSameRangeWalksNothing`, `.ATickerTheFilingsIndexDoesNotCarryIsAttemptedOnceAndNotAskedAgain` |
| A sweep that throws on the fourth ticker, over the same eight members | item 58, item 59 | The `SRLFLW001..008` pool again, with the handler serving three tickers whole and then sending the shape that ended 3.9's day four: a page carrying `links.next` followed by an empty page, so `GetAllPagesAsync` leaves its loop without `serverRanOut` and refuses the partial history [A20, D-71]. **It still throws after item 59 and by a different route**, which is why it was left as it stands: its handler offers `links.next` on both pages against a `meta.total` of 4, so the walk now ends on the addressable-offset bound rather than on the empty page, and the bound leaves `serverRanOut` false exactly as the old break did. **The property is what the exception path leaves behind**: the three walked whole keep their attempt rows, the one that threw does not, and the run stops rather than walking past a failure. **This is a measured loss rather than an imagined one.** Day four walked 443 members, committed their filings, and wrote no attempt row at all, because the rows were accumulated and flushed after the loop; the halt path reached that flush and the exception path did not, so the next run would have re-bought the coverage at about 83.3 units a member. **Known to fire**: reverting the per-ticker write was run, and the assertion reads `[]` against the three expected. The failure is the provider's real shape rather than a synthetic throw, so it also stands as the regression pin for item 59 | `FlowSweepTests.ASweepThatThrowsKeepsTheAttemptRowsOfEveryTickerItWalkedWhole` |
| A stale allowance reading beside an exhausted one | item 47 | Two `AllowanceRule.Decide` calls over the same 100,000-unit limit and 50,000 reserve: 49,995 spent on the day being run, and 1,957 spent stamped the day before. **The verdicts are the rule's own rather than two decisions the test built**, which is what makes this an assertion about the halt line rather than about two strings. The lines are asserted unequal before anything is asserted about their wording, because two lines can each carry the right words and still be the same line; then each is checked to name its own verdict, to carry the figure an operator acts on, and to keep the shared half about the ticker and the resumption. A third case pins a halt with no decision kept, which renders as a lost verdict rather than an empty reason. **This exists because the two collapsed and it cost a diagnosis**: the halt at 00:40Z on 2026-08-19 was `Stale` against an allowance 98 percent unspent, and telling it from `Exhausted` took a hand-written read of `/api/user` | `FlowRangeTests.AStaleReadingAndAnExhaustedOneProduceDifferentHaltLines`, `.AHaltWithNoDecisionKeptSaysSoRatherThanRenderingNothing` |
| Seven transaction dates a filings payload can carry | item 49 | One filing whose derivative array holds a date in 2033, a date in the year 24, a two-digit year, an impossible month and day, a full timestamp, an empty string and no field at all. **The decisive case is the two-digit year, which comes back null**: it is what proves the `0024-01-01` row in the store came from the payload rather than from a parse widening `24` to `0024`, which was item 49's open question. The timestamp is taken by its first ten characters, the one leniency. **Nothing borrows `filed_at`**, which is the substitution that would turn an unknown transaction date into a plausible one nobody could later question. The fixture pins storing-what-was-sent, so the authored decision on whether to bound the column has a test to fail | `FlowIngestorTests.TheParseStoresTheDateItWasSentAndNullsWhatItCannotRead` |
| A ticker refused between its two distribution calls | item 33, 3.10 | `SRLEVT001..008` plus a delisted name with bars in the window, against a handler billing one unit a call. Seeded at 49,990 spent, nine units are available and a ticker is two, so four are covered and the fifth is refused. **"Both calls or neither" is what this asserts and what no parse test can see**: every dispatched ticker was asked for `splits/{t}` and `div/{t}`, eight calls over four names, and the refused one was asked for neither and carries no attempt row. A half-covered ticker behind an attempt row saying it was covered is indistinguishable from an ordinary name by row count. The empty-array case is a name with no distributions, attempted once and not re-asked, which 3.1 measured on `SPY.US` itself | `EventsSweepTests.AGatedSweepLeavesTheRefusedTickerWithNeitherCallNorAnAttemptRow`, `.ACompletedSweepCoversTheDelistedHalfAndReInvokesToNothing`, `.ATickerWithNoDistributionsIsAttemptedOnceAndNotAskedAgain` |
| The database the suite is actually connected to, and the routes it could have taken | item 26, 3.13 | Not a seeded payload: the fixture is the running process's own connection and the suite's own source tree. Four assertions at runtime, that the database name ends `_tests` and differs from the supplied one, that it is never `stockresearcherlab_ci` which `ci.ps1` drops, that `current_database()` read off the open connection is the derived name, and that `meta.test_database` carries the marker preparation refuses to proceed without. Two over the sources, that `ConfigurationBuilder`, `AddJsonFile` and `GetConnectionString` appear in `TestDatabase.cs` alone and that every `new NpgsqlConnection(` in the suite is built from `TestDatabase.ConnectionString`; both patterns are whitespace-tolerant and both are quoted in their own failure message [`CLAUDE.md` §7]. **The fifth runtime one is the defect that prompted this**: config resolves before any test asks for it, which had been true only because a class in another collection happened to seed in parallel, and was false often enough to turn 23 CI runs' worth of green into a coin toss | `TestDatabaseIsolationTests.TheSuiteRunsAgainstADerivedDatabaseRatherThanTheSuppliedOne`, `.TheSuiteDatabaseIsNeverTheOneCiPs1Drops`, `.TheConnectionReachesTheDatabaseTheSuiteDerived`, `.TheDatabaseCarriesTheMarkerTheSuiteStamps`, `.ConfigIsSeededBeforeAnyTestAsksForIt`, `.NoTestSourceBuildsAConnectionStringOfItsOwn`, `.EveryConnectionTheSuiteOpensComesFromTestDatabase` |
| A duplicate holder reaching the write | item 21 | Six pool members served three holder entries over two institutions, run through the stage rather than the parse. Unguarded it is `Npgsql.PostgresException 21000: ON CONFLICT DO UPDATE command cannot affect row a second time`, observed by removing the guard and running it, which is what makes the guard known to be load-bearing rather than merely present. Guarded it writes twelve rows, reports six drops in the run log, and `BlackRock Inc` reads back at 900 shares. The clean payload beside it reports twelve rows and a **zero** drop count, because a guard chosen against zero observations is only measured if its zero appears | `FundamentalsRotationTests.ADuplicateHolderIsOneRowWithTheLargerHoldingAndIsCountedInTheLog`, `.TheCoverageLineCarriesTheHoldingsCountAndItsCollisionCount`, `.HoldingRowsAreNotCountedIntoTheAttemptRecord` |
| An order with a source cut out of it | 3.16 | `BackfillSequence.SourceOrder` copied with `PercentileEngine` removed, run through the same comparison the real assertion uses rather than a copy of it. The omission stands for the class: a component gains a range mode, nobody adds it to the order, and a full backfill completes reporting the row counts of the eleven that did run while one table keeps whatever the nightly runs put there. C11 is the worst instance because it is last and ranks what the four above it wrote, so the first thing to read the gap is a screen in phase 4 | `BackfillSequenceTests.TheCheckFailsOnAnOrderWithASourceCutOutOfIt`, and the direction it protects at `.EveryRegisteredStageWithARangeModeIsInTheOrder` |
| The store as item 60 measured it, as counts by date | item 60 | Twenty-four measured `price_daily` date counts newest first, 2026-08-17 back to 2026-07-15, with a twenty-date tail behind them so the median branch is the one exercised rather than the not-enough-history one. **The decisive three are the top**: 2026-08-17, 2026-08-14 and 2026-08-13 hold exactly one bar each, `SPY.US` alone, against 3,024 on 2026-08-12, which is D-104's benchmark load having run forward past where the equity sweep stopped. A frontier reading the newest date present returns 2026-08-17 and every compute stage then writes against three sessions that never had a population; the rule returns 2026-08-12 and names what it walked past. The boundary is pinned beside it at exactly `0.95 * 3,000` and one bar below, because an inclusive floor and an exclusive one differ on one date and on nothing else | `PriceFrontierRuleTests.TheFrontierIsTheNewestDateCarryingARealBarCountRatherThanTheNewestDatePresent`, `.ASettledNewestDateIsTheFrontierAndTheAbsenceOfSkippedDatesIsStated`, `.ADateWithLessThanAFullWindowBehindItIsTakenAndTheSkippedCheckIsSaidSo`, `.AnEmptyTableHasNoFrontier`, `.TheFloorIsInclusive` |
| A store whose newest dates carry the benchmark and nothing else | item 60 | `SRLFRNT001..025` on every date from 2030-01-01 to 2030-02-09, then `SRLFRNT001` alone on 2030-02-10, 11 and 12: the measured shape rebuilt in a store rather than in a list of counts. **The dates are in 2030 deliberately and it is the isolation that requires it**: the frontier is a property of the whole of `price_daily` and this class is not in the `database` collection, so a fixture that made the frontier earlier would change what every concurrent class runs against. Rows newer than every other fixture can only move it forward, which nothing depends on, and the refusal is then produced by asking for an end past a high frontier rather than by lowering one. **Both directions are asserted and the second is the half that would go missing**: a guard refusing everything passes the refusal test, so the frontier itself is asserted accepted with the real date set read back, not merely a date well inside it. The fourth assertion is the placement, that a stage which never resolves the calendar is not refused at all, because a check at the top of `BackfillRun.RunAsync` would pass the other three and make the backfill unable to extend the store | `BackfillFrontierTests.ARangeEndPastTheFrontierIsRefused`, `.ARangeEndingAtTheFrontierIsAccepted`, `.TheRefusalFailsTheRunAndLandsInTheRunLog`, `.AStageThatNeverResolvesTheCalendarIsNotRefusedAtAll` |
| Reader disagreeing with its Reads cell, both ways | 3.5.1 | `RecordInspector` declaring `proposal`, which §3 does not name for it, against `RecordInspector` declaring nothing where its cell names three. The stage version of this fixture existed from the reconciliation; the reader version is separate because the merged map is what is new, and a merge that silently dropped the reader source would leave C36 unchecked while every assertion stayed green [D-109] | `ReadDeclarationConformanceTests.BothDirectionsFailOnAReaderThatDisagreesWithTheCatalogue`, with `.TheMergedMapCarriesStagesAndReadersBoth` guarding the merge itself |
| Nine names, each failing exactly one universe criterion | 3.5.1 | One ticker per criterion in D-4's own test order, each clearing every criterion the evaluation reaches before the one it is built for, plus a tenth clearing all nine. **The tenth is not decoration**: nine rejections with nothing admitted is also what a C01 that rejects everything produces, and it is the only assertion separating the two. The three pre-pass names carry full fundamentals deliberately, so a loop testing the projected verdict late would admit them and `NoPrePassFailureIsAdmitted` would fail [D-108] | `UniverseRejectionTests.EachCriterionIsRecordedAgainstTheNameBuiltToFailIt`, `.TheNameThatClearsEveryCriterionIsAMemberAndCarriesNoRejection`, `.NoPrePassFailureIsAdmitted` |
| A rejection row the evaluation no longer produces | 3.5.1 | A planted row at the evaluation date for a ticker the fixture never rejects. An upsert cannot remove a row, so without the delete a name that becomes a member stays listed as rejected forever and `SCHEMA.md`'s "a member has no row" quietly stops holding [D-108, D-68] | `UniverseRejectionTests.ANameThatStopsBeingRejectedLosesItsRow`, with `.ARerunOfOneEvaluationDateProducesIdenticalRows` |
| Two config versions either side of the viewed date | 3.5.1 | `universe.min_price` at 5 from 2020-01-01 and 8 from 2022-06-01, read through `ConfigResolution.Resolve` rather than a stub of the rule, at a date before each. A viewer resolving as of now would show today's floor beside a 2022 verdict, which is correct-looking and wrong on every historical date [D-43, INVARIANT 13] | `RecordInspectorTests.AThresholdTakesTheVersionInForceOnTheViewedDate`, `.AKeyNotYetInForceReadsAbsentRatherThanTheNewestValue`, `.EveryThresholdIsResolvedAsOfTheDateBeingViewed` |
| A reader that declares no write at all | 3.5.1 | `RecordInspector.Access()` itself, every declared table against every `WriteOperation`. Asserted over the object the constructor uses rather than a rebuild of its arguments, because a constructor passing a different list would leave the guard unchecked while the test passed. This is the read-only guarantee as a property of the construction rather than a rule someone keeps [D-109] | `RecordInspectorTests.EveryWriteOnEveryDeclaredTableIsRefused`, `.ATableOutsideTheDeclarationIsNotReadable`, `.TheGuardAndTheDeclaredReadSetAreTheSameList` |

## Fixtures the design already calls for

These are named in other documents as things that must be provable. They become rows
above when they are written.

- A ticker whose two benchmark windows do not intersect, which is the divergence the
  replay done-when records as not claimed and no fixture reaches [item 52, BUILD_PLAN
  phase 3's replay line]. **What it has to contain**: a member on the replayed date
  whose own price history carries a hole long enough that its last `RequiredBars` of
  272 bars, which C08 takes by row count with no lower date bound, end before the
  benchmark window opens, that window being `CompositePadDays` of 400 calendar days
  before the range start on the range path and the run date on the nightly path. With
  no overlap `Relative` yields null at every offset and all four benchmark-relative
  columns go null at once while erroring on nothing. `OBNK.US` is the measured
  instance at an 873-day hole, and it cannot serve: its last bar is 2026-02-05, so it
  is not a member on any date the replay can use, which is why the class needs a
  built fixture rather than a real name. **The obligation is listed and the fixture is
  not built**, because which of the two bounds is correct is unasked and a fixture
  asserting either would answer it by implication
- The worked arbitration example in `ARCHITECTURE.html` §10, which must reproduce
  exactly [BUILD_PLAN phase 7]
- A prefix snapshot, asserting one hash across a full night of calls [phase 6]
- A dossier with a deliberately corrupted citation, which the validator must reject
  [phase 6]
- A proposal whose probability contradicts its own stop and target [phase 6]
- A stale end-of-day file, which the freshness guard must abort on [phase 1]
- A fundamental whose filing date is later than its period end, asserting no read
  before the filing date [phase 1]
- A fundamental whose filing date equals its period end [phase 1]
- A fundamental whose filing date is null [phase 1]
- A fundamental whose filing date precedes its own period end [phase 1]
- A ticker with fewer than four clean gaps, which the universe must exclude
  [phase 1]
- An `events` row whose `event_date` was not yet public on the date it is read
  back for, which is the earnings calendar's point-in-time gap and has no fixture
  because the source carries no date on which a schedule was announced
  [BUILD_PLAN carried obligations, 1 owed to 5]
- A night where the primary digest provider is unavailable and the chain falls
  through [phase 5]
- A night where no digest provider is healthy and the run halts [phase 5]
- A candidate surfaced by two screens, receiving two screen blocks [phase 6]
- An acquired candidate with no 63-day price series [phase 8]
- A delisted candidate [phase 8]
- A full-abstention night, asserting exposure matching holds at zero [phase 7]
