#requires -Version 5.1
<#
    seed.ps1 - insert the configuration rows the pipeline reads at startup.

    STUB at 0.1. It seeds config_rows from CONFIG_REFERENCE.md, the portfolio
    registry from D-36, and the digest provider chain from D-25. All three are
    configuration rather than code, which is what lets a screen be added without
    writing a class [CLAUDE.md section 5]. None of it exists to seed yet.
#>

Write-Host "seed.ps1: stub, nothing to seed until config_rows exists [0.1]. Exiting 0."
exit 0
