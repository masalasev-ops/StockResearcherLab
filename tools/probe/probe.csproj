<Project Sdk="Microsoft.NET.Sdk">

  <!-- Phase P only. A measuring instrument, deleted or rewritten afterwards
       [CLAUDE.md section 4]. Deliberately not one of the six source projects in
       D-56. Target framework and language settings come from
       Directory.Build.props; package versions from Directory.Packages.props. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" />
  </ItemGroup>

  <ItemGroup>
    <!-- Never committed. Copied beside the binary so the configuration builder
         finds it at run time [D-55]. -->
    <None Update="appsettings.Secrets.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
